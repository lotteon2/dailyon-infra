apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  type: ClusterIP
  ports:
    - targetPort: 8082
      port: 8082
  selector:
    app: notification-service
    profile: dev
